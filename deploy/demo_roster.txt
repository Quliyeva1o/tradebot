# Which *_Demo tasks may place real orders, and ON WHOSE ACCOUNT.
# Format: <task name> <broker>   # comment. One bot per symbol, no exceptions.
#
# WHY THIS FILE EXISTS: a .bat exists for every strategy/symbol pair, so
# install_tasks.ps1 registers eighteen tasks. Enabling all of them puts TWO
# Demo bots on GER40, JP225 and XAUUSD, and two bots on one symbol do not
# co-operate -- _partition_positions() sees the other's position as foreign and
# blocks entry, so whichever polls first wins and the other logs
# foreign_position_blocks_entry forever. That happened on XAUUSD on 2026-09-08
# (37 blocked entries in the log) and again on the VPS build on 2026-09-10.
#
# The allocation was a real decision, made per symbol on the numbers, but it
# lived only in Task Scheduler's enabled/disabled state -- nowhere in the repo.
# Rebuild from scratch and it was silently lost. Now it is version-controlled.
#
# Anything NOT listed here is disabled by install_tasks.ps1. Paper tasks are
# unaffected: they use per-strategy state files, never collide, and running
# both sides on a symbol is how the losing config stays measurable.

# 2026-09-20: CUT FROM SIX BOTS TO ONE, and --reverse-on-stop removed from every launcher.
# A 1-year and a 3-month replay of every ORB breakout configuration, on both this account's
# broker and CFI's (ORB_BREAKOUT_1Y_BROKER_REPORT.md, ORB_BREAKOUT_3MO_BROKER_REPORT.md,
# scripts/orb_breakout_year_backtest.py) found:
#   - five of the six symbols lost over the last 12 months AND again over the last 3:
#     DJI30, SPX500, NDX100, JP225 and GER40 together -107.5R on CFI prices, PF 0.61-0.90.
#   - --reverse-on-stop 0.5 cost 34.6R over the year across the four Demo bots whose Paper
#     twin shares their parameters, and on XAUUSD -- the one symbol that makes money -- it
#     turned +44.2R into +39.2R while RAISING max drawdown from 18.8R to 23.1R. It needs a
#     66.7% win rate (1R risked for 0.5R) and gets 62.3%.
#   - only XAUUSD on 60m OR / M1 / 3R with --weekend-flat was positive in BOTH windows:
#     +34.5R, PF 1.49, 5.8% max drawdown over the year, +2.4R over the last quarter.
# The five losing configurations keep running as PAPER bots. That is how they stay measurable,
# and this decision gets revisited on their numbers rather than on memory.
#
# KNOW WHAT THE SURVIVOR IS. Over FundingPips' full 2009-2026 history (the longer feed) this
# same config had NO edge before 2020: PF 0.98 for 2009-2019 even with no swap charged at all
# (1448 trades), and PF 1.25-1.40 from 2020 on (860 trades) depending on the swap assumption.
# The replay charges today's swap on every year, which overstates the low-rate years, so the
# truth lies between those bounds. Its edge belongs to the current regime; judge it live
# against that, and expect a decade like 2009-2019 to be roughly breakeven or worse.

# 2026-09-21: RISK HALVED TO 0.25%, AND A STOP RULE FIXED BEFORE THE FIRST LIVE TRADE.
# From the 1-year CFI replay of this bot's Paper twin (identical strategy flags;
# artifacts/orb_1y/cfi/OrbBreakoutwf_XAUUSD_Paper_trades.csv, 125 trades, +34.5R):
#   - The edge decays inside that year: +0.484R/trade over the first 59 trades (to 2026-03-22),
#     +0.090R/trade over the last 66. If the first-half edge still held, a second half that weak
#     has p = 0.029 (bootstrap). Expect roughly +0.09 to +0.28R/trade forward, not PF 1.49.
#   - Size is the lever, because a drawdown stop catches collapse, not decay: a zero-edge copy of
#     this bot breaches its p95 drawdown only 30% of the time within 40 trades.
#   - STOP -- take OrbBreakoutwf_XAUUSD_Demo out of this file -- if, counted from its first live
#     trade, drawdown from peak exceeds 17R within 40 trades or 22R within 80 (each the p99 of
#     the year's bootstrap envelope), or if at the 40-trade checkpoint (about 4 months) the net is
#     below -5.8R (its p05).
#   These are set now, before any live data, on purpose. Do not loosen them after seeing trades.
#   At 0.25% about 1 trade in 125 computes below CFI's 0.01-lot minimum and is clamped up to it,
#   risking up to 0.5% on that trade: the R figures stay true, the dollars run slightly hot.

# 2026-09-21: TWO MACHINES, TWO BROKERS -- AND REAL ORDERS ONLY ON ONE.
# The VPS trades the CFI account; this workstation stays on FundingPips. Both machines run the
# same launchers (they are in git), and each resolves the broker's own ticker from its own .env
# (config/brokers.py), so nothing below is a launcher name any more -- it is a permission, and
# permission is per account.
#
# Every number that justifies the bot below was measured on CFI: its spread, its swap, its
# 0.01-lot minimum, the 1-year replay the stop rule's bootstrap envelope comes from. None of it
# transfers to FundingPips, which charges $5/lot commission, caps gold at 5 lots and is a prop
# TRIAL account. So the same launcher on the FundingPips machine is a DIFFERENT deployment and
# runs PAPER until someone does that work for it. install_tasks.ps1 asks this machine which
# broker it is and disables every Demo task not listed for it -- on the workstation, all of them.

# 2026-09-22: A SECOND REAL BOT, ON A SYMBOL NO OTHER DEMO BOT TRADES -- NDX100 FIRST FVG.
# FvgWindow_NDX100_Paper has run since 2026-09-15. Backtested on CFI's own US100_Spot M1
# (scripts/fvg_window_envelope.py, the launcher's exact flags), 2019-03..2026-09:
#   - 732 trades, PF 1.140, +76.9R with CFI's spread AND today's swap charged; PF 1.200, +106.8R
#     without swap. The truth lies between: today's swap on every year overstates the low-rate ones.
#   - swap is the real cost here: 45% of trades are held overnight and a long pays $106.69 per lot
#     per night. After swap the LONG side is flat (PF 0.998, -0.8R over 409 trades) and every R of
#     the edge comes from the SHORTS (PF 1.342, +77.7R). Both sides stay on: picking a side after
#     seeing this is the selection the fib/sweep research warned against.
#   - last 12 months +17.0R (PF 1.27), last 6 months -2.8R, 2025 -2.4R. Positive in 5 of 8 years.
#   Weaker than the XAUUSD bot (+34.5R, PF 1.49 over its year); what it adds is a second symbol.
#
# STOP -- take FvgWindow_NDX100_Demo out of this file -- if, counted from its first live trade,
# drawdown from peak exceeds 24.8R within 40 trades or 34.7R within 80, or if at the 40-trade
# checkpoint (about 6 months at ~7 trades a month) the net is below -14.1R. Each is the p99 / p05
# of the full-history bootstrap (deploy/kill_rules.json). Set before deployment; do not loosen.
# (Adopted as 25.1 / 35.4 / -14.3; tightened the same day, before any live trade -- see below.)
#   These are wide because the strategy is: a 29% win rate at 3R means long losing runs are normal.
#   The same bootstrap stops a copy of the backtest's own edge 5% of the time by trade 40, a copy
#   with ZERO edge only 11% by trade 40 and 12% by trade 80, one losing 0.2R a trade 38% by trade 80.
#   So, as for the XAUUSD bot, this rule catches collapse, not decay, and size is the real lever:
#   0.25% risk, so 25R is 6.25% of the account. At 0.25% the stop is never wide enough for the
#   0.01-lot minimum to bind (it would take a stop of more than 625 points on this account).

# 2026-09-22: EVERY NUMBER ABOVE WAS MEASURED ON A CLOCK AN HOUR OFF FOR ~3 WEEKS A YEAR.
# Both brokers' servers run New York close (New York + 7h, on the AMERICAN daylight-saving dates),
# not the Europe/Bucharest this repo assumed (config/brokers.py SERVER_CLOCKS has the measurement).
# In the weeks when Europe and the US change clocks on different dates, every replay built the
# 09:30 range and the 10:00 FVG window from bars an hour early. Re-run on the right clock, same
# windows and method:
#   - OrbBreakoutwf_XAUUSD, the year behind its stop rule: 119 trades, +31.5R, PF 1.47 (was 125,
#     +34.5R, 1.49). Edge decay unchanged: +0.492R/trade to 2026-03-22, +0.062R after (was +0.484
#     and +0.090). Envelope 17.4R/40, 22.3R/80, -5.9R@40 against 17.3/22.2/-5.8 by the same
#     bootstrap on the old year -- noise, so its rule stays 17 / 22 / -5.8. Last quarter unchanged,
#     +2.4R. Over its whole CFI history it weakens: +21.7R, PF 1.03 (was +41.6R, PF 1.06).
#   - the five cut symbols, their current launchers on CFI: -56.7R over the year, -38.5R over the
#     last quarter. Still losing in both windows.
#   - FvgWindow_NDX100, full history with swap: 728 trades, PF 1.149, +81.4R (was 732, 1.140,
#     +76.9R); last 12 months +13.1R, PF 1.21 (was +17.0R, 1.27). Its envelope is TIGHTER on every
#     clause, and the bot had no live trade yet, so its rule took the corrected numbers above.

# 2026-09-22: A DEMO BOT ON EVERY SYMBOL -- THE USER'S CALL, WITH THE EVIDENCE WRITTEN DOWN.
# Asked for: put as many paper bots on real Demo orders as possible, and where a symbol has several
# candidates take the one with the best result. One bot per symbol still holds (see the top of this
# file), so XAUUSD and NDX100 keep theirs -- each already the best on its symbol -- and the four free
# symbols each get their best paper bot, ranked by the last year on CFI (2025-09-19..2026-09-18, the
# window every decision above used). CFI's replay, New York close clock, full = 2025-01..2026-09
# (the real M1 CFI has for these indices):
#                            last year               full history        last quarter
#   DJI30   inverse   <-     50 tr PF 1.07  +0.7R    85 tr PF 0.86  -2.9R    +0.5R
#           breakout         50 tr PF 0.76 -11.3R    85 tr PF 0.97  -2.5R    -3.9R
#   SPX500  inverse   <-     61 tr PF 1.23  +2.5R    99 tr PF 0.90  -2.3R    +1.3R
#           breakout         61 tr PF 0.66 -20.4R    99 tr PF 0.90  -8.9R    -6.7R
#   GER40   sweep     <-     26 tr PF 1.21  +2.6R    38 tr PF 1.60 +10.1R    -2.7R
#           breakout        115 tr PF 0.95  -4.7R   203 tr PF 1.24 +40.3R    -2.9R
#           sweep inv        26 tr PF 0.58  -6.7R    38 tr PF 0.66  -8.2R    +2.2R
#   JP225   sweep     <-     33 tr PF 0.98  -0.5R    49 tr PF 0.84  -5.6R    +0.8R
#           breakout        183 tr PF 0.96  -6.1R   316 tr PF 1.03  +6.6R   -18.9R
#           inverse         181 tr PF 0.83  -7.5R   313 tr PF 0.77 -18.0R    +3.7R
#
# KNOW WHAT THESE FOUR ARE. None has the evidence the two bots above had when they were deployed:
#   - the inverse bots win only while the breakout they mirror loses (strategy/inverse.py): over
#     FundingPips' 6.7 years the six inverse bots pooled PF 0.76. On CFI both are negative over the
#     full 21 months. Each trade risks the breakout's 4R target to win its 1R stop: a win rate
#     near 80% is needed and any regime change in the breakout hurts twice.
#   - JP225's best candidate LOST over the year; every JP225 candidate did. It is here because
#     the instruction was one bot per symbol, not because it has an edge.
#   - GER40 sweep is the one with a positive year AND full history, on 26 and 38 trades -- a
#     sample so small that its stop rule below is tight by construction.
# Risk 0.25%, like the two bots above: none of the four earned more.
#
# STOP -- take a bot out of this file -- on the same terms as above, each counted from its first
# live trade: drawdown from peak beyond its p99 within 40 or 80 trades, or net below its p05 at the
# 40-trade checkpoint. Bootstrap (100000 draws, seed 20260922) of its full CFI history, the same
# function the NDX100 rule came from (scripts/fvg_window_envelope.envelope). Numbers in
# deploy/kill_rules.json; set before any live trade, do not loosen:
#   OrbBreakoutinv_DJI30_Demo   10.6R/40  15.7R/80  -7.1R@40
#   OrbBreakoutinv_SPX500_Demo  10.3R/40  15.0R/80  -6.8R@40
#   OrbSweep_GER40_Demo         12.0R/40  14.6R/80  -1.4R@40
#   OrbSweep_JP225_Demo         26.9R/40  40.8R/80 -19.0R@40
# At 20-40 trades a year for the three thinner ones, the 40-trade checkpoint is one to two years out.

# 2026-09-22: FUNDINGPIPS PLACES REAL ORDERS TOO -- THE USER'S CALL.
# This ends the 2026-09-21 rule above that the FundingPips account stays paper. The user asked for
# the strategies to trade on both accounts. The same selection was run on FundingPips' own prices
# (its spread, swap, and $5/lot commission on gold), last year 2025-09-19..2026-09-18 on the New York
# close clock, and it picks the SAME six bots, so both accounts run one launcher set:
#                            last year on FundingPips      (runner-up)
#   XAUUSD  breakout wf      118 tr PF 1.46 +31.1R         breakout 60m/3R +20.6R, sweep +3.9R
#   NDX100  First FVG         85 tr PF 1.20 +12.5R         inverse -0.7R, breakout -10.2R
#   DJI30   inverse           51 tr PF 0.98  -0.2R         breakout -14.7R
#   SPX500  inverse           61 tr PF 0.99  -0.1R         breakout -18.5R
#   GER40   sweep             27 tr PF 0.96  -0.6R         breakout -1.7R, sweep inverse -6.5R
#   JP225   sweep             33 tr PF 0.81  -4.6R         breakout -8.1R, inverse -8.6R
# On FundingPips four of the six LOST the year: they are here because the instruction was one bot per
# symbol on both accounts. Its longer history (since 2019) is harsher still on the inverse bots:
# DJI30 inverse PF 0.78 (-24.0R, 459 trades), SPX500 inverse PF 0.74 (-31.0R, 479 trades).
# FundingPips is a prop TRIAL account; its own loss limits apply on top of the rules below.
#
# STOP rules for the FundingPips account, fixed before its first order (deploy/kill_rules.json,
# key "fundingpips"), each on the same basis as the task's CFI rule but on FundingPips' prices:
#   OrbBreakoutwf_XAUUSD_Demo   17.6R/40  22.6R/80  -6.1R@40    (1-year replay, as on CFI)
#   FvgWindow_NDX100_Demo       26.3R/40  37.9R/80 -15.6R@40    (2019-03..2026-09, swap charged)
#   OrbBreakoutinv_DJI30_Demo   11.4R/40  17.2R/80  -7.8R@40    (2019-01..2026-09)
#   OrbBreakoutinv_SPX500_Demo  12.0R/40  18.2R/80  -8.6R@40    (2019-01..2026-09)
#   OrbSweep_GER40_Demo         19.6R/40  27.8R/80 -11.8R@40    (2022-01..2026-09)
#   OrbSweep_JP225_Demo         25.3R/40  38.3R/80 -17.6R@40    (2024-04..2026-09)
# A task listed for both accounts has one line per account below, and each account's weekly report
# judges its own bots by its own rules.

# 2026-09-23: ON CFI, GER40 MOVES FROM THE SWEEP TO THE 30m BREAKOUT -- THE USER'S CALL.
# Asked for: where a paper bot has a better backtest than the symbol's Demo bot, swap them, on CFI.
# Every paper bot was replayed on both brokers (full / 3 years / 1 year / 3 months / 1 month); GER40
# was the only symbol where the paper candidate was clearly stronger. SPX500 and DJI30 were left on
# their inverse bots by the user although the breakout wins over FundingPips' longer history.
#                         CFI 2025-01..2026-09      FundingPips 2022-01..2026-09   last year (CFI)
#   GER40 breakout  <-    203 tr PF 1.24 +40.3R     609 tr PF 1.29 +142.4R        116 tr PF 0.94 -5.7R
#         sweep            38 tr PF 1.60 +10.1R     122 tr PF 1.04   +2.9R         26 tr PF 1.21 +2.6R
# The sweep's edge rests on 38 trades and was +2.9R over nearly five years on FundingPips; the
# breakout's on 600+, in every year from 2023 to 2025 (+69.0 / +38.3 / +62.4R on FundingPips).
# KNOW WHAT IT COSTS: it lost 2022 (-19.1R) and is losing 2026 (-8.2R FP, -6.7R CFI), and its
# drawdowns are several times the sweep's -- 19.9R max on CFI, 34.2R on FundingPips, against 4.1R
# and 9.7R. The last year still favours the sweep; the swap was made on the longer evidence.
# Risk 0.25% like every other Demo bot. The sweep keeps running on CFI as a Paper bot, and stays
# the Demo bot on FundingPips, which this change does not touch.
#
# STOP -- take OrbBreakout_GER40_Demo off CFI -- if, from its first live trade, drawdown from peak
# exceeds 27.5R within 40 trades or 37.9R within 80, or the net is below -14.4R at trade 40.
# Bootstrap of its full CFI history with today's swap (100000 draws, seed 20260922,
# scripts/fvg_window_envelope.envelope); deploy/kill_rules.json. Set before deployment; do not loosen.
# The sweep's CFI rule (12.0 / 14.6 / -1.4) retired with it; its FundingPips rule stands.

# --- the bots allowed to place real orders ---
OrbBreakoutwf_XAUUSD_Demo    cfi    # CFI XAUUSD_, 60m OR / M1 / 3R, weekend-flat, no reverse, 0.25% risk
FvgWindow_NDX100_Demo        cfi    # CFI US100_Spot, 10:00 First FVG, M15 limit, 3R, 0.25% risk
OrbBreakoutinv_DJI30_Demo    cfi    # CFI US30_SPOT, inverse of 60m OR / M1 / 4R breakout, 0.25% risk
OrbBreakoutinv_SPX500_Demo   cfi    # CFI US500_SPOT, inverse of 60m OR / M1 / 4R breakout, 0.25% risk
OrbBreakout_GER40_Demo       cfi    # CFI GER30_SPOT, 30m OR / M1 / 4R breakout, 0.25% risk (was the sweep until 2026-09-23)
OrbSweep_JP225_Demo          cfi    # CFI JPN225_SPOT, 15m OR sweep / M15 / to 11:00 / 2R, 0.25% risk

OrbBreakoutwf_XAUUSD_Demo    fundingpips    # FundingPips XAUUSD, same launcher
FvgWindow_NDX100_Demo        fundingpips    # FundingPips NDX100
OrbBreakoutinv_DJI30_Demo    fundingpips    # FundingPips DJI30
OrbBreakoutinv_SPX500_Demo   fundingpips    # FundingPips SPX500
OrbSweep_GER40_Demo          fundingpips    # FundingPips GER40
OrbSweep_JP225_Demo          fundingpips    # FundingPips JP225
